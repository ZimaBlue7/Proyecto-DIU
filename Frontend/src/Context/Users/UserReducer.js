import {
} from '../type';

const UserReducer = (state, action) => {
    const {payload, type} = action

    switch(type){
        default:
            return state;
    }

}

export default UserReducer;