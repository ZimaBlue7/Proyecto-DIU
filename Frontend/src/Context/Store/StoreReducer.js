import {
} from '../type';

const StoreReducer = (state, action) => {
    const {payload, type} = action

    switch(type){
        default:
            return state;
    }

}

export default StoreReducer;