import {
} from '../type';

const CarritoReducer = (state, action) => {
    const {payload, type} = action

    switch(type){
        default:
            return state;
    }

}

export default CarritoReducer;